#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: first intiger num
 * @b: second intiger num
 * Return: mult
 */
int mul(int a, int b)
{
	int mult;

	mult = (a * b);
	return (mult);
}
